services: 
  catalogue:
    build:
      dockerfile: catalogue.Dockerfile
    image: lokesh_lkh/catalogue:1.0.0
    container_name: catalogue
    depends_on:
    - mongodb



  mongodb:
    build: 
      dockerfile: mongodb.Dockerfile #it moves to mongodb directory.
    image: lokesh_lkh/mongodb:1.0.0  # docker build -t lokesh/mongodb:1.0.0
    container_name: mongodb

networks:
  default:
    driver: bridge
    name: roboshop
    external: false  # if true, we need to create network manually.